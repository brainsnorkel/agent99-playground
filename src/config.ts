/**
 * Default URL for the LLM endpoint.
 * This can be overridden by the LLM_URL environment variable.
 * 
 * Default: localhost:1234 (standard LM Studio port)
 * Change this to your LM Studio server address if running on a different machine.
 */
export const DEFAULT_LLM_URL = 'http://localhost:1234/v1';
